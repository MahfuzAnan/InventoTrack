<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product View</title>
    <style>
        .product-card {
            border: 1px solid #ddd;
            padding: 15px;
            margin: 10px;
            width: calc(33.33% - 20px);
            float: left;
        }

        .product-image {
            max-width: 100%;
            height: 150px;
            object-fit: cover;
        }

        .clearfix::after {
            content: "";
            clear: both;
            display: table;
        }

        .view-button {
            display: block;
            margin-top: 10px;
            padding: 8px 15px;
            background-color: #1885d8;
            color: white;
            text-align: center;
            text-decoration: none;
            cursor: pointer;
        }
    </style>
</head>

<body>

    <h1>Product View</h1>

    <div class="clearfix">
        <% products.forEach(product=> { %>
            <div class="product-card">
                <img class="product-image" src="/uploads/<%= product.image %>" alt="<%= product.name %>">
                <h2>
                    <%= product.name %>
                </h2>
                <p>
                    <%= product.category %>
                </p>
                <a class="view-button" href="/product/<%= product._id %>">View Product Info</a>
            </div>
            <% }); %>
    </div>

</body>

</html>